# Existing config
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=postgres
quarkus.datasource.password=postgres
quarkus.datasource.reactive.url=postgresql://localhost:5432/fp_demo

# Add these
quarkus.datasource.reactive.max-size=20
quarkus.datasource.reactive.idle-timeout=PT10M

# Enable debug logging
quarkus.log.category."io.vertx.pgclient".level=DEBUG
quarkus.log.category."io.vertx.sqlclient".level=DEBUG