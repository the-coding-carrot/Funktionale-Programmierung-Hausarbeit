#include "cover.typ"
#set page(
  margin: (top: 4cm),
  header: align(right)[
    #pad(
      image("images/DHBW-Logo.svg"),
      top: 1cm
    )
    #line(length: 100%)
  ]
)
#show heading: set block(below: 1em)

#include "content.typ"
